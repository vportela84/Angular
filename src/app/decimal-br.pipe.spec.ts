import { DecimalBrPipe } from './decimal-br.pipe';

describe('DecimalBrPipe', () => {
  it('create an instance', () => {
    const pipe = new DecimalBrPipe();
    expect(pipe).toBeTruthy();
  });
});
